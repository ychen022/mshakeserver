Import mealshake.sql before using.

Database connection settings are in db.php.

Start website from index.php, login with any credential from the SQL table users.

After a successful login, the previously selected matching options will be automatically displayed on the left side of the page.[dynamic content]

Navigation bar is not fully dynamic yet. Click "Min Zhang" on the top right to call out the user functionalities--only sign out is working at the moment.